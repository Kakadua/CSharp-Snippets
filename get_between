public String get_between(String content, String before, String after){
	String[] temp = explode(before, content);
	if(!String.IsNullOrEmpty(temp[1])){
		temp = explode(after, temp[1]);
			return temp[0];
	}
	else{
		return "";
	}
}